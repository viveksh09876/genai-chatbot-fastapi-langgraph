# 🤖 LangGraph AI Agent Chatbot (FastAPI + Streamlit)

This project is an **AI Agent Chatbot** built using **LangGraph (ReAct Agent)** and exposed through a **FastAPI backend** with a **Streamlit UI frontend**.

It supports:
- **Groq models** (Llama / Mixtral)
- **OpenAI model** (`gpt-4o-mini`)